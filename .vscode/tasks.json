{

    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install packages",
            "type": "shell",
            "windows": {
                "command": "${workspaceFolder}\\setup.bat"
            },
            "group": "build"
        }
    ]
}